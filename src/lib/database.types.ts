/**
 * Supabase-generated database types — PLACEHOLDER for MI-8.
 *
 * The schema doesn't exist yet (it lands in MI-9), so this is an empty shape
 * that keeps the typed client compiling. Regenerate it once the schema exists:
 *
 *   npx supabase gen types typescript --project-id <project-ref> \
 *     > src/lib/database.types.ts
 *
 * (See the Open Considerations on MI-8 / the schema work in MI-9.)
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};
